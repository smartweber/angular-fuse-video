import { DisplayAsTextareaPipe } from './display-as-textarea.pipe';

describe('DisplayAsTextareaPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayAsTextareaPipe();
    expect(pipe).toBeTruthy();
  });
});
